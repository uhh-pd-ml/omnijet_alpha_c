dataset_kwargs_train:
  logger_name: IterDataset-Train
  shuffle_files: true
  shuffle_data: true

dataset_kwargs_val:
  logger_name: IterDataset-Validation
  shuffle_files: false
  shuffle_data: true

dataset_kwargs_test:
  logger_name: IterDataset-Test
  shuffle_files: false
  shuffle_data: true

batch_size: 128
data_dir: beegfs/desy/user/korcariw/multiRes_CaloClouds/dataset/
